<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/refreshView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:background="?attr/selectableItemBackground"
        android:drawablePadding="4dp"
        android:text="@string/refresh_another_perfume"
        android:textAppearance="@style/TextAppearance.SeeHyangComponents.Body2"
        android:textColor="@color/colorGrey4"
        app:drawableEndCompat="@drawable/ic_refresh"
        app:drawableTint="@color/colorGrey4" />

</FrameLayout>